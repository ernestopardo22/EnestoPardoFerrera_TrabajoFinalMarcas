info:
  name: GuardarOrigenReceta
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5008/guardar-origen-receta
  body:
    type: json
    data: |-
      {
            "autoctono": false
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
