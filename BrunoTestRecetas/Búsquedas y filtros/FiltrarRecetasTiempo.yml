info:
  name: FiltrarRecetasTiempo
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5008/recetas/filtrar/tiempo?min=20&max=25
  params:
    - name: min
      value: "20"
      type: query
    - name: max
      value: "25"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
