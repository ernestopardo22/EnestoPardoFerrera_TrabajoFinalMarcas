info:
  name: RecetasOrdenarCampo
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5008/recetas/ordenar/tiempo?orden=desc
  params:
    - name: orden
      value: desc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
